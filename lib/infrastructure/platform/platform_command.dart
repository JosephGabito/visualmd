/// Commands contributed by the host application's native menu system.
enum PlatformCommand {
  newWorkspace,
  openWorkspace,
  openReaderSources,
  saveWorkspace,
  saveWorkspaceAs,
  addFolder,
  addMarkdown,
}
