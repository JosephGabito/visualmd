/// Commands contributed by the host application's native menu system.
enum PlatformCommand {
  newWorkspace,
  openWorkspace,
  openReaderSources,
  openSampleLibrary,
  saveWorkspace,
  saveWorkspaceAs,
  addFolder,
  addMarkdown,
}
